rootProject.name = 'mapbot'

include ':app'

include ':lib-retry-support'
project(':lib-retry-support').projectDir = new File(settingsDir, '/libs/retry-support')

include ':lib-slack'
project(':lib-slack').projectDir = new File(settingsDir, '/libs/slack')
